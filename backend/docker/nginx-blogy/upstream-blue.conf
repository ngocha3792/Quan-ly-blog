# Nội dung tĩnh, có commit — port/container name không đổi giữa các lần
# deploy, chỉ upstream-current.conf (symlink, không commit) trỏ vào file
# nào mới đổi.
upstream blog_api {
    server api-blue:8080;
}
